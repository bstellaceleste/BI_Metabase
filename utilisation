cloner le projet en local
ouvrir le cmd dans le répertoire local
taper "npm install" pour installer les dépendances
puis "npm start"
ouvrir ensuite le navigateur en localhost sur le port 3000
créer un compte utilisateur et entrer les informations de connexion à votre bd pour y avoir accès depuis metabase
pour que les requête préécrites affichent les résultats il faut se connecter à la bd d'odoo, donc il faut au préalable avoir installé odoo en local également
